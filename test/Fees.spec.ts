describe('Fees', () => {
  it('takes proper fees', async () => {
    // set the fees on the fork created pool
    // Added fake liquidity (give tokens, add/create position)
    // Make sure fee breaks up right for protocol
  })
})
